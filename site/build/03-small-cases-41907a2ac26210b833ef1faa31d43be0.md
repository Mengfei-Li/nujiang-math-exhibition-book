# 先用手算攻下 k=1,2,3,4

在启动大型求解器之前，先解决最小目标。小例子既训练构造，也帮助检查程序模型是否写对。

## 一个三角形：E(n,1)=7

固定六边没有三角形，因此至少要加一条边，总边数至少 7：

$$
E(n,1)\ge7.
$$

加入 23 后出现三角形 123，所以 7 边已经够：

$$
E(n,1)\le7.
$$

上下界相遇：

$$
\boxed{E(n,1)=7}.
$$

## 两个三角形：E(n,2)=8

只加一条边时，它的两个端点在固定图 $H$ 中最多只有一个公共邻居，所以最多闭合一个三角形；因此 7 边不够两个。

加入 23、24 后出现 123 与 124，总边数 8，故

$$
\boxed{E(n,2)=8}.
$$

## 三个与四个三角形：都只需 9 边

加入

$$
23,\quad24,\quad34,
$$

顶点 1、2、3、4 之间六条边全有，形成 $K_4$。它含

$$
\binom43=4
$$

个三角形。因此 9 边同时满足 $k=3$ 和 $k=4$。

为什么 8 边不能有 3 个？因为相对固定图只加两条边。逐类检查两条新增边是否共享端点，可知最多闭合两个三角形。于是

$$
\boxed{E(n,3)=E(n,4)=9}.
$$

## 为什么同一个边数覆盖两个目标？

$E(n,k)$ 要“至少 $k$ 个”。9 边构造直接产生 4 个三角形，所以当然也满足至少 3 个。阶梯函数会出现平台：

$$
k=3,4\quad\longmapsto\quad E=9.
$$

## 看—试—译—证—辨—讲

| 节奏 | 本章动作 |
|---|---|
| 看 | 观察四叶星的开口 |
| 试 | 在互动图中依次加23、24、34 |
| 译 | 一条边闭合公共邻居形成的开口 |
| 证 | 构造给上界，少边不可能给下界 |
| 辨 | 题面九边示例不是 $k=1$ 的最优证明 |
| 讲 | 30秒解释7、8、9、9 |

## 自测

1. 为什么 7 边见证不能单独证明 $E(n,1)=7$？
2. $K_4$ 中为什么有 4 个三角形？
3. $E(n,3)=9$ 是否表示每个 9 边图都有 3 个三角形？

<details><summary>参考答案</summary>

1. 见证只给 $E(n,1)\le7$；还要用固定六边无三角形证明少于7不行。  
2. 四个顶点中任取三个，共 $\binom43=4$ 组，每组两两相连。  
3. 不是。它说存在9边可行图，且8边不可能达到目标；并非所有9边图都可行。

</details>

## 30 秒答辩

> 固定六边无三角形。加23得到第一个，所以 $E(n,1)=7$；再加24得到两个，所以 $E(n,2)=8$。再加34后1、2、3、4形成 $K_4$，一次有4个三角形，而两条新增边最多只有2个，所以 $E(n,3)=E(n,4)=9$。

